# CSV to SQLite Universal Converter - Dependencies
# Compatible with Python 3.8+, Windows (admin-free)

pandas>=1.2.0
sqlite3; python_version<"3.10"

# Optional but recommended for production
tqdm>=4.50.0  # Progress bars
openpyxl>=3.0.0  # Excel support (if needed)
